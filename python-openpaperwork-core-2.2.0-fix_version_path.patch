--- a/pyproject.toml
+++ b/pyproject.toml
@@ -22,9 +22,9 @@ lint = [
 ]
 
 [tool.setuptools_scm]
-root = ".."
+root = "."
 relative_to = "__file__"
-write_to = "openpaperwork-core/src/openpaperwork_core/_version.py"
+write_to = "src/openpaperwork_core/_version.py"
 
 [build-system]
 requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
